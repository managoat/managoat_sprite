#!/bin/sh
# The desktop's only network interface is its authenticated loopback HTTP app.
# Keep distribution disabled even when launching the embedded release directly.
export RELEASE_DISTRIBUTION=none
export RELEASE_COOKIE=unused
export ERL_CRASH_DUMP=/dev/null
